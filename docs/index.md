# django-borg

> *"You must comply."*

AI-bootstrapped, vote-curated schema mapping for Django. Assimilate heterogeneous supplier data into your canonical schema -- AI proposes mappings, humans curate them, votes accumulate, and once a mapping is trusted it runs deterministically.
